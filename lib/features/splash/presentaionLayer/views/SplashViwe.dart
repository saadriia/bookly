import 'package:flutter/material.dart';

import 'widgets/SplashViweBody.dart';

class SplashViwe extends StatelessWidget {
  const SplashViwe({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SplashViweBody(),
    );
  }
}
